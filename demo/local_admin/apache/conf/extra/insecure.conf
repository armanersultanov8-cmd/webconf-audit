# Intentionally insecure override for demo/validation purposes.

# Wrapped in IfModule to verify parser handles it gracefully.
<IfModule mod_status.c>
    <Location "/server-status">
        SetHandler server-status
    </Location>
</IfModule>
